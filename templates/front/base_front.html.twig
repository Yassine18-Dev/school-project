<!DOCTYPE html>
<html lang="fr" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ArenaMind - E-Sports Intelligence{% endblock %}</title>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        :root {
            --bg-dark: #0b0e17;
            --bg-card: #15192b;
            --accent-purple: #8833ff;
            --accent-blue: #3b82f6;
            --text-primary: #ffffff;
            --text-secondary: #94a3b8;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .navbar {
            background-color: rgba(11, 14, 23, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            padding: 1rem 0;
        }

        .navbar-brand {
            font-weight: 800;
            letter-spacing: -0.5px;
            font-size: 1.5rem;
        }

        .nav-link {
            color: var(--text-secondary) !important;
            font-weight: 500;
            margin: 0 0.5rem;
            transition: all 0.2s;
        }

        .nav-link:hover, .nav-link.active {
            color: var(--text-primary) !important;
            text-shadow: 0 0 10px rgba(136, 51, 255, 0.3);
        }

        .btn-primary {
            background: linear-gradient(90deg, #8833ff 0%, #6366f1 100%);
            border: none;
            font-weight: 600;
            padding: 0.6rem 1.5rem;
            border-radius: 50px;
        }

        .btn-primary:hover {
            opacity: 0.9;
            box-shadow: 0 0 15px rgba(136, 51, 255, 0.4);
        }

        main { flex: 1; }

        .footer {
            background-color: var(--bg-card);
            border-top: 1px solid rgba(255, 255, 255, 0.05);
            padding: 3rem 0;
            margin-top: 5rem;
        }

        /* Utils */
        .text-purple { color: var(--accent-purple); }
        .bg-card { background-color: var(--bg-card); }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_front_home') }}">
                <img src="{{ asset('assets/images/logo.svg') }}" alt="ArenaMind" height="50">
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_front_home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_front_games') }}">Jeux</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_front_tournaments') }}">Tournois</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_front_predictions') }}">Prédictions IA</a>
                    </li>
                </ul>
                <div class="d-flex gap-2">
                    <a href="{{ path('app_login') }}" class="btn btn-outline-light rounded-pill px-4">Connexion</a>
                    <a href="{{ path('ui_register') }}" class="btn btn-primary">Rejoindre</a>
                </div>
            </div>
        </div>
    </nav>

    <main>
        <div class="container mt-3">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible fade show" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
        {% block body %}{% endblock %}
    </main>

    <footer class="footer">
        <div class="container text-center">
            <h4 class="fw-bold mb-3">ArenaMind</h4>
            <p class="text-muted mb-4">La plateforme ultime pour les tournois et prédictions E-Sports.</p>
            <div class="d-flex justify-content-center gap-3 mb-4">
                <a href="#" class="text-secondary"><i class="fa-brands fa-discord fa-lg"></i></a>
                <a href="#" class="text-secondary"><i class="fa-brands fa-twitter fa-lg"></i></a>
                <a href="#" class="text-secondary"><i class="fa-brands fa-twitch fa-lg"></i></a>
            </div>
            <p class="small text-muted mb-0">&copy; {{ 'now'|date('Y') }} ArenaMind. Tous droits réservés.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
