{% extends 'base.html.twig' %}

{% block title %}Modifier Tournoi - ArenaMind Admin{% endblock %}

{% block body %}
<div class="container py-4" style="max-width: 800px;">
    
    <div class="admin-header">
        <div class="admin-icon rounded-circle bg-warning bg-opacity-10 p-3 text-warning">
            <i class="fa-solid fa-pen-to-square fa-2x"></i>
        </div>
        <div>
            <h1 class="admin-title mb-0">Modifier le Tournoi</h1>
            <p class="text-secondary mb-0">Ajustez les détails de la compétition #{{ tournoi.id }}.</p>
        </div>
    </div>

    <div class="mt-5">
        {{ include('tournoi/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    
    <div class="mt-5 pt-4 border-top border-secondary border-opacity-25">
         {{ include('tournoi/_delete_form.html.twig') }}
    </div>

</div>
{% endblock %}