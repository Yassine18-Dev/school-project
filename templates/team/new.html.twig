{% extends 'base.html.twig' %}
{% block title %}New Team{% endblock %}

{% block body %}
  {% set actions %}
    <a class="am-btn" href="{{ path('app_team_index') }}">Retour</a>
  {% endset %}

  {% set body %}
    {{ include('team/_form.html.twig', { form: form, button_label: 'Créer' }) }}
  {% endset %}

  {{ include('_ui/page_card.html.twig', {
    title: 'Nouvelle équipe',
    subtitle: 'Créer une équipe',
    actions: actions,
    body: body
  }) }}
{% endblock %}

{% block stylesheets %}
  {{ parent() }}
  {{ include('_ui/am_styles.html.twig') }}
{% endblock %}
