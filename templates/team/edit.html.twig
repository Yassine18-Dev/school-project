{% extends 'base.html.twig' %}
{% block title %}Edit Team{% endblock %}

{% block body %}
  {% set actions %}
    <a class="am-btn" href="{{ path('app_team_index') }}">Retour</a>
  {% endset %}

  {% set body %}
    {{ include('team/_form.html.twig', { form: form, button_label: 'Mettre à jour' }) }}
    <div class="am-formActions">
      {{ include('team/_delete_form.html.twig') }}
    </div>
  {% endset %}

  {{ include('_ui/page_card.html.twig', {
    title: 'Modifier équipe',
    subtitle: 'Mettre à jour les informations',
    actions: actions,
    body: body
  }) }}
{% endblock %}

{% block stylesheets %}
  {{ parent() }}
  {{ include('_ui/am_styles.html.twig') }}
{% endblock %}
