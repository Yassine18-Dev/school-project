{% extends 'base.html.twig' %}
{% block title %}Player{% endblock %}

{% block body %}
  {% set actions %}
    <a class="am-btn" href="{{ path('app_player_index') }}">Retour</a>
    <a class="am-btnPrimary" href="{{ path('app_player_edit', {'id': player.id}) }}">Edit</a>
  {% endset %}

  {% set body %}
    <div class="am-tableWrap">
      <table class="am-table">
        <tbody>
          <tr><th>ID</th><td>{{ player.id }}</td></tr>
          <tr><th>Nom</th><td>{{ player.name }}</td></tr>
        </tbody>
      </table>
    </div>

    <div class="am-formActions">
      {{ include('player/_delete_form.html.twig') }}
    </div>
  {% endset %}

  {{ include('_ui/page_card.html.twig', {
    title: 'Détails joueur',
    subtitle: 'Informations',
    actions: actions,
    body: body
  }) }}
{% endblock %}

{% block stylesheets %}
  {{ parent() }}
  {{ include('_ui/am_styles.html.twig') }}
{% endblock %}
