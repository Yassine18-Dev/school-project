{% extends 'base.html.twig' %}

{% block title %}Modifier le Tournoi - ArenaMind{% endblock %}

{% block body %}
<div class="container py-5">
    
    {# En-tête de la page de modification #}
    <div class="text-center mb-5 animate__animated animate__fadeInDown">
        <div class="d-inline-block rounded-circle p-3 mb-3" 
             style="background: rgba(255, 193, 7, 0.1); border: 2px solid #ffc107; box-shadow: 0 0 15px rgba(255, 193, 7, 0.2);">
            <i class="fa-solid fa-pen-to-square fa-2x text-warning"></i>
        </div>
        
        <h1 class="admin-title" style="font-family: 'Orbitron', sans-serif; letter-spacing: 3px; font-weight: 700; color: white;">
            MODIFIER LE <span style="color: var(--accent-blue);">TOURNOI</span>
        </h1>
        
        <p class="text-muted mx-auto" style="max-width: 500px;">
            Identifiant : <span class="badge bg-dark border border-secondary text-info">#{{ tournoi.id }}</span><br>
            Mettez à jour les informations du tournoi ci-dessous.
        </p>
    </div>

    {# Conteneur du formulaire #}
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
            <div class="animate__animated animate__fadeInUp">
                {# On réutilise le formulaire avec un label de bouton différent #}
                {{ include('tournoi/_form.html.twig', {
                    'button_label': '💾 Enregistrer les modifications'
                }) }}
            </div>
            
            {# Zone de danger : Suppression #}
            <div class="mt-5 p-4 rounded-4 border border-danger border-opacity-25 bg-danger bg-opacity-10">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="text-danger mb-1">Zone de danger</h5>
                        <p class="text-muted small mb-0">Cette action est irréversible. Toutes les données seront perdues.</p>
                    </div>
                    {{ include('tournoi/_delete_form.html.twig') }}
                </div>
            </div>

            <div class="text-center mt-4">
                <a href="{{ path('app_tournoi_index') }}" class="text-decoration-none text-muted small hover-white transition">
                    <i class="fa-solid fa-arrow-left me-1"></i> Annuler et retourner à la liste
                </a>
            </div>
        </div>
    </div>

</div>
{% endblock %}