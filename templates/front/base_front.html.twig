<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{% block title %}ArenaMind • Front{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="{{ asset('css/cart.css') }}">
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">


  <style>
    :root{
      --bg1:#050814; --bg2:#1b1f3b;
      --card:rgba(20,24,60,.95);
      --border:#312e81;
      --text:#fff; --muted:#9ca3af;
      --purple:#a855f7; --blue:#3b82f6;
      --danger:#dc2626; --success:#16a34a;
    }
    body{margin:0;font-family:'Segoe UI',sans-serif;background:radial-gradient(circle at top,var(--bg2),var(--bg1));color:var(--text);}
    nav{background:rgba(5,8,20,.75);border-bottom:1px solid rgba(49,46,129,.35);padding:14px 22px;display:flex;justify-content:space-between;align-items:center;position:sticky;top:0;backdrop-filter:blur(10px);}
    .logo{font-weight:900;font-size:20px;color:var(--purple);}
    nav a{color:white;text-decoration:none;font-weight:700;margin-left:14px;font-size:14px;opacity:.92;}
    nav a:hover{opacity:1;color:#dbeafe;}
    .container{max-width:460px;margin:56px auto;background:var(--card);padding:34px;border-radius:18px;box-shadow:0 0 40px rgba(168,85,247,.25);border:1px solid rgba(49,46,129,.55);}
    h1{text-align:center;margin:0 0 8px;color:var(--purple);}
    .subtitle{text-align:center;color:var(--muted);margin:0 0 24px;font-size:14px;}
    label{font-size:13px;color:#cbd5f5;}
    input,select{width:100%;padding:12px;margin:6px 0 16px;background:#0b0f2e;border:1px solid var(--border);border-radius:12px;color:white;outline:none;}
    button{width:100%;padding:12px;background:linear-gradient(90deg,var(--purple),var(--blue));border:none;border-radius:14px;color:white;font-weight:900;cursor:pointer;}
    button:hover{opacity:.92;}
    .link{text-align:center;margin-top:16px;font-size:14px;color:var(--muted);}
    .link a{color:#60a5fa;text-decoration:none;font-weight:700;}
    .badge{display:inline-block;padding:6px 12px;border-radius:999px;background:rgba(168,85,247,.15);border:1px solid rgba(168,85,247,.3);font-size:12px;margin-right:6px;}
    .flash-success{background:var(--success);padding:10px;border-radius:10px;margin-bottom:12px;text-align:center;font-weight:700;}
    .flash-error{background:var(--danger);padding:10px;border-radius:10px;margin-bottom:12px;text-align:center;font-weight:700;}
    .err{color:#fecaca;font-size:13px;margin:-8px 0 10px;}
  </style>
    

</head>
<body>

<nav>
  <div class="logo">🎮 ArenaMind</div>
    <div class="nav-right">
    <a href="{{ path('ui_login') }}">Login</a>
    <a href="{{ path('ui_register') }}">Register</a>
    <a href="{{ path('ui_profile') }}">Profile</a>
    <a href="{{ path('ui_profile_edit') }}">Edit</a>
    <a href="{{ path('ui_forgot_password') }}">Forgot</a>
    <a class="nav-link" href="/shop/merch">Shop</a>
    <a href="{{ path('ui_logout') }}">Logout</a>
    <a href="{{ path('ui_admin') }}">Admin</a>
    
        <button id="cart-btn" class="cart-btn">
        <i class="fa-solid fa-cart-shopping"></i>
        <span id="cart-count">0</span>
    </button>
  </div>
</nav>
<div id="cart-panel" class="cart-panel">
    <h6 class="text-uppercase text-info mb-3">Mon panier</h6>

    <div id="cart-items">
        <small class="empty-cart">Panier vide</small>
    </div>

    <div class="cart-footer">
        <div class="d-flex justify-content-between fw-bold">
            <span>Total</span>
            <span><span id="cart-total">0</span> €</span>
        </div>

        <a href="/cart" class="checkout-btn">💳 Passer commande</a>
    </div>
</div>

<div class="container">
  {% for msg in app.flashes('success') %}<div class="flash-success">{{ msg }}</div>{% endfor %}
  {% for msg in app.flashes('error') %}<div class="flash-error">{{ msg }}</div>{% endfor %}
  {% block body %}{% endblock %}
</div>

{% block javascripts %}
  <script src="{{ asset('js/cart.js') }}"></script>

  <script src="{{ asset('js/payment.js') }}"></script>
{% endblock %}



  
</body>

</html>
