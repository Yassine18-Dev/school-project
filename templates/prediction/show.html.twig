{% extends 'base.html.twig' %}

{% block title %}Prediction{% endblock %}

{% block body %}
    <h1>Prediction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prediction.id }}</td>
            </tr>
            <tr>
                <th>ScorePredit</th>
                <td>{{ prediction.scorePredit }}</td>
            </tr>
            <tr>
                <th>DatePrediction</th>
                <td>{{ prediction.datePrediction ? prediction.datePrediction|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prediction_index') }}">back to list</a>

    <a href="{{ path('app_prediction_edit', {'id': prediction.id}) }}">edit</a>

    {{ include('prediction/_delete_form.html.twig') }}
{% endblock %}
