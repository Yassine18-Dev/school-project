<form method="post" action="{{ path('app_shop_product_delete', {'id': shop_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shop_product.id) }}">
    <button class="btn">Delete</button>
</form>
