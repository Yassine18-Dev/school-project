{% extends 'base.html.twig' %}

{% block title %}Nouveau Tournoi - ArenaMind{% endblock %}

{% block body %}
<div class="container py-5">
    
    {# En-tête de la page avec icône et titres stylisés #}
    <div class="text-center mb-5 animate__animated animate__fadeInDown">
        <div class="d-inline-block rounded-circle p-3 mb-3" 
             style="background: rgba(136, 51, 255, 0.1); border: 2px solid var(--accent-purple); box-shadow: 0 0 15px rgba(136, 51, 255, 0.3);">
            <i class="fa-solid fa-trophy fa-2x text-white"></i>
        </div>
        
        <h1 class="admin-title" style="font-family: 'Orbitron', sans-serif; letter-spacing: 3px; font-weight: 700; color: white;">
            CRÉER UN <span style="color: var(--accent-purple);">TOURNOI</span>
        </h1>
        
        <p class="text-muted mx-auto" style="max-width: 500px;">
            Remplissez les informations ci-dessous pour lancer votre nouvelle compétition sur la plateforme.
        </p>
    </div>

    {# Conteneur du formulaire centré #}
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
            <div class="animate__animated animate__fadeInUp">
                {# Inclusion du formulaire réutilisable #}
                {{ include('tournoi/_form.html.twig', {
                    'button_label': '🏆 Lancer le Tournoi'
                }) }}
            </div>
            
            {# Lien de retour discret sous le formulaire #}
            <div class="text-center mt-4">
                <a href="{{ path('app_tournoi_index') }}" class="text-decoration-none text-muted small hover-white transition">
                    <i class="fa-solid fa-chevron-left me-1"></i> Retour à la liste des tournois
                </a>
            </div>
        </div>
    </div>

</div>

<style>
    /* Animation fluide pour les liens de retour */
    .hover-white {
        transition: all 0.3s ease;
    }
    .hover-white:hover {
        color: white !important;
        letter-spacing: 0.5px;
    }
</style>
{% endblock %}