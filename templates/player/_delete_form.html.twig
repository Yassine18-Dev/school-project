<form method="post" action="{{ path('app_player_delete', {'id': player.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
  <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player.id) }}">
  <button class="am-btn">Supprimer</button>
</form>
